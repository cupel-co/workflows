name: Pull Request Update

on:
  workflow_call:
    inputs:
      environment:
        description: The environment name to inject into the pull request description.
        required: true
        type: string
    outputs:
      description:
        description: The rendered pull request description.
        value: '${{ jobs.update.outputs.description }}'

jobs:
  update:
    name: Update
    runs-on: ubuntu-latest
    outputs:
      description: '${{ steps.update.outputs.description }}'
    steps:
      - name: Update
        id: update
        uses: cupel-co/actions/.github/actions/pull-request/update@890cd45dab0f39bc1a680ba03cbe63d08a6d3756
        with:
          environment: ${{ inputs.environment }}
