name: Terrakube Destroy

on:
  workflow_call:
    inputs:
      environment:
        description: The target environment where the destroy operation will be executed (e.g., staging, production).
        required: true
        type: string
      destroy-args:
        description: Additional arguments to pass to the tofu destroy command.
        required: false
        type: string
      init-args:
        description: Additional arguments to pass to the tofu init command.
        required: false
        type: string
      version:
        description: The version of OpenTofu to install.
        default: 1.11.6
        required: false
        type: string
      terrakube-hostname:
        description: The hostname of your Terrakube instance.
        default: terrakube-api.cicd.cupel.co
        required: false
        type: string
      working-directory:
        description: The directory where the infrastructure code is located.
        default: ./infrastructure
        required: false
        type: string
    secrets:
      destroy-args:
        description: Additional arguments to pass to the tofu destroy command.
        required: false
      init-args:
        description: Additional arguments to pass to the tofu init command.
        required: false
      terrakube-token:
        description: The API token for authenticating with Terrakube.
        required: true

jobs:
  destroy:
    name: Destroy
    runs-on: ubuntu-latest
    environment: '${{ inputs.environment }}'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Destroy
        uses: cupel-co/actions/.github/actions/terrakube/destroy@v0.28.2
        with:
          destroy-args: '${{ inputs.destroy-args }} ${{ secrets.destroy-args }}'
          init-args: '${{ inputs.init-args }} ${{ secrets.init-args }}'
          terrakube-hostname: '${{ inputs.terrakube-hostname }}'
          terrakube-token: '${{ secrets.terrakube-token }}'
          version: '${{ inputs.version }}'
          working-directory: '${{ inputs.working-directory }}'
