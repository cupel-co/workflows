name: Pull Request Update

on:
  workflow_call:
    inputs:
      environment:
        description: The environment name to inject into the pull request description.
        required: true
        type: string
    outputs:
      description:
        description: The rendered pull request description.
        value: '${{ jobs.update.outputs.description }}'

jobs:
  update:
    name: Update
    runs-on: ubuntu-latest
    outputs:
      description: '${{ steps.update.outputs.description }}'
    steps:
      - name: Update
        id: update
        uses: cupel-co/actions/.github/actions/pull-request/update@510dd9e13ec97d330add71a78f7519f51657ada3
        with:
          environment: ${{ inputs.environment }}
