name: Pull Request Update

on:
  workflow_call:
    inputs:
      environment:
        description: The environment name to inject into the pull request description.
        required: true
        type: string
    outputs:
      description:
        description: The rendered pull request description.
        value: '${{ jobs.update.outputs.description }}'

jobs:
  update:
    name: Update
    runs-on: ubuntu-latest
    outputs:
      description: '${{ steps.update.outputs.description }}'
    steps:
      - name: Update
        id: update
        uses: cupel-co/actions/.github/actions/pull-request/update@d6ada7c3046e8a1509a6c333da8e950a6354e427
        with:
          environment: ${{ inputs.environment }}
